#include <iostream>
using namespace std;
int main() {
    int num,i,sum=0;
    cout << "Enter the number of numbers= ";
    cin >> num;
    for(i=0;i<=num;i++){
        sum += i;
    }
    cout << "The sum of number is = " << sum; 
    
    
    return 0;
}
